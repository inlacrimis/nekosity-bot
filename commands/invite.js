exports.run = (config, bot, Discord, embed, COLORS, bot, neko, responses) => {
  message.channel.send("",
  embed.setTitle("Invite link:"),
  embed.setDescription("https://bit.ly/2OIzzNF")

)};
